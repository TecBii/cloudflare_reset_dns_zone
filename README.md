# Cloudflare Reset DNS Zone
This script can be used, if you want to reset Cloudflare DNS records for a specific domain.

You can find the Zone ID and create the API key in your selected domain -> Overview.